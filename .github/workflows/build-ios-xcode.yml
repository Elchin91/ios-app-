name: Build iOS IPA with Xcode

on:
  workflow_dispatch:

jobs:
  build:
    name: Build IPA with Xcode
    runs-on: macos-latest
    timeout-minutes: 60
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Prebuild iOS project
        run: |
          npx expo prebuild --platform ios --clean || \
          (echo "⚠️ Prebuild failed, trying without clean..." && npx expo prebuild --platform ios)

      - name: Install CocoaPods dependencies
        run: |
          cd ios
          pod install
          cd ..

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - name: List available SDKs
        run: |
          echo "=== Available SDKs ==="
          xcodebuild -showsdks
          echo ""
          echo "=== Available Schemes ==="
          xcodebuild -list -workspace ios/M10Wallet.xcworkspace || xcodebuild -list -project ios/M10Wallet.xcodeproj

      - name: Build Archive
        run: |
          mkdir -p ${{ github.workspace }}/build
          
          # Пробуем найти доступную версию iOS SDK
          echo "🔍 Ищем доступные SDK..."
          xcodebuild -showsdks
          IOS_SDK=$(xcodebuild -showsdks | grep -o 'iphoneos[0-9.]*' | head -n 1 | sed 's/iphoneos//' || echo "")
          
          if [ -z "$IOS_SDK" ]; then
            echo "⚠️ Не найдена версия SDK, используем iphoneos"
            SDK_ARG="-sdk iphoneos"
          else
            echo "✅ Используем SDK: iphoneos$IOS_SDK"
            SDK_ARG="-sdk iphoneos$IOS_SDK"
          fi
          
          echo "🔨 Начинаем сборку архива..."
          echo "Пробуем с destination generic/platform=iOS..."
          
          # Пробуем разные варианты destination
          ARCHIVE_SUCCESS=false
          
          # Вариант 1: с generic/platform=iOS
          if xcodebuild archive \
            -workspace ios/M10Wallet.xcworkspace \
            -scheme M10Wallet \
            -configuration Release \
            $SDK_ARG \
            -destination "generic/platform=iOS" \
            -archivePath ${{ github.workspace }}/build/M10Wallet.xcarchive \
            CODE_SIGN_IDENTITY="-" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            PROVISIONING_PROFILE_SPECIFIER="" 2>&1; then
            ARCHIVE_SUCCESS=true
            echo "✅ Архив успешно создан (вариант 1)"
          else
            echo "⚠️ Вариант 1 не удался, пробуем вариант 2..."
            
            # Вариант 2: без destination, но с указанием SDK
            if xcodebuild archive \
              -workspace ios/M10Wallet.xcworkspace \
              -scheme M10Wallet \
              -configuration Release \
              $SDK_ARG \
              -archivePath ${{ github.workspace }}/build/M10Wallet.xcarchive \
              CODE_SIGN_IDENTITY="-" \
              CODE_SIGNING_REQUIRED=NO \
              CODE_SIGNING_ALLOWED=NO 2>&1; then
              ARCHIVE_SUCCESS=true
              echo "✅ Архив успешно создан (вариант 2)"
            else
              echo "⚠️ Вариант 2 не удался, пробуем вариант 3 (build вместо archive)..."
              
              # Вариант 3: используем build вместо archive
              xcodebuild build \
                -workspace ios/M10Wallet.xcworkspace \
                -scheme M10Wallet \
                -configuration Release \
                $SDK_ARG \
                -destination "generic/platform=iOS" \
                CODE_SIGN_IDENTITY="-" \
                CODE_SIGNING_REQUIRED=NO \
                CODE_SIGNING_ALLOWED=NO
              
              # Создаем архив вручную из build продукта
              BUILD_DIR="ios/build/Release-iphoneos"
              if [ -d "$BUILD_DIR/M10Wallet.app" ]; then
                mkdir -p ${{ github.workspace }}/build/M10Wallet.xcarchive/Products/Applications
                cp -r "$BUILD_DIR/M10Wallet.app" ${{ github.workspace }}/build/M10Wallet.xcarchive/Products/Applications/
                ARCHIVE_SUCCESS=true
                echo "✅ Архив создан вручную из build продукта"
              fi
            fi
          fi
          
          # Проверяем, что архив создан
          if [ "$ARCHIVE_SUCCESS" = false ] || [ ! -d "${{ github.workspace }}/build/M10Wallet.xcarchive" ]; then
            echo "❌ Архив не был создан!"
            echo "Содержимое build директории:"
            ls -la ${{ github.workspace }}/build/ || true
            exit 1
          fi
          echo "✅ Архив найден: ${{ github.workspace }}/build/M10Wallet.xcarchive"

      - name: Export IPA
        run: |
          mkdir -p ${{ github.workspace }}/build/ipa
          # Пробуем экспорт с ad-hoc
          if xcodebuild -exportArchive \
            -archivePath ${{ github.workspace }}/build/M10Wallet.xcarchive \
            -exportPath ${{ github.workspace }}/build/ipa \
            -exportOptionsPlist ${{ github.workspace }}/exportOptions.plist \
            -allowProvisioningUpdates 2>&1; then
            echo "✅ IPA экспортирован через xcodebuild"
          else
            echo "⚠️ xcodebuild export не удался, создаем IPA вручную..."
            # Создаем правильную структуру IPA для Trollstore
            PAYLOAD_DIR="${{ github.workspace }}/build/ipa/Payload"
            mkdir -p "$PAYLOAD_DIR"
            cp -r "${{ github.workspace }}/build/M10Wallet.xcarchive/Products/Applications/M10Wallet.app" "$PAYLOAD_DIR/"
            cd "${{ github.workspace }}/build/ipa"
            zip -r M10Wallet.ipa Payload
            rm -rf Payload
            echo "✅ IPA создан вручную: ${{ github.workspace }}/build/ipa/M10Wallet.ipa"
          fi

      - name: Upload IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: M10Wallet-IPA-Xcode
          path: build/ipa/*.ipa
          retention-days: 30

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: build/ipa/*.ipa
          tag_name: v${{ github.run_number }}-xcode
          name: Release v${{ github.run_number }} (Xcode Build)
          body: |
            IPA файл собран через Xcode для установки через Trollstore
            
            ## Инструкция по установке:
            1. Скачайте файл M10Wallet.ipa
            2. Откройте Trollstore на вашем устройстве
            3. Выберите "Install IPA File"
            4. Выберите скачанный файл
            5. Дождитесь завершения установки
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

